source "https://rubygems.org"

# Local preview only. GitHub Pages runs its own Jekyll on push.
# We use a newer Jekyll locally because github-pages pins to Jekyll 3.9,
# which is incompatible with Ruby 3.4+ (csv dropped from stdlib).
gem "jekyll", "~> 4.4"
gem "csv"
gem "base64"
gem "logger"
